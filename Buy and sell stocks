def stock(nums):
    while True:
        sp=max(nums[1:])
        sp_index=nums.index(sp)
        cp=min(nums[:sp_index])
        cp_index=nums.index(cp)
        if(sp>cp):
            return sp-cp
        else:
            return 0
    
prices = [7,6,4,3,1]
print(stock(prices))
